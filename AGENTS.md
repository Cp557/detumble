Write clean, maintainable, functional code with the simplest effective solution. Favor clear data flow, small reusable helpers, and behavior that is easy to reason about. Keep code structure clean and avoid unnecessary abstraction.

Be simple, clear and concise with conversational responses to the user.

See `docs/REFERENCE.md` for the canonical technical reference. If you make any big code or architecture changes, update this file.

Keep `docs/CONCEPTS.md` updated as the project introduces new aerospace, physics, math, or programming concepts. Write these learning notes in simple, clear, and concise language.

When the user says `git acp`, they mean git add, commit, and push the current changes. Use `git push origin main` for the push step.
